from fastapi import FastAPI

# Создаём экземпляр приложения.
# app — это и есть наше веб-приложение, к нему мы будем "привязывать" обработчики.
app = FastAPI()


# Декоратор @app.get("/") говорит:
# "когда придёт GET-запрос на адрес '/' (корень сайта) — вызови функцию ниже".
@app.get("/")
def read_root():
    # Возвращаемый словарь FastAPI автоматически превратит в JSON-ответ.
    return {"message": "Привет! Сервер работает."}