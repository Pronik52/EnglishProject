---
type: "query"
date: "2026-07-24T10:04:09.412702+00:00"
question: "Проверь, правильно ли я сделал по третьему пункту, что ты описал?"
contributor: "graphify"
outcome: "useful"
source_nodes: ["login()", "get_current_user()", "api()", "logout()", "enterApp()"]
---

# Q: Проверь, правильно ли я сделал по третьему пункту, что ты описал?

## Answer

Expanded from graph vocab: [api, auth, current, fetch, local, login, logout, request, response, set, test, token, user]. Review of current diff: transition is incomplete and currently breaks login. app/routers/auth.py sets HttpOnly SameSite=Lax cookie and max_age aligns with JWT, but returns an empty 200 response; frontend/js/auth.js still expects JSON access_token and writes localStorage, so tok is null. app/auth.py still uses OAuth2PasswordBearer with auto_error=True and never reads request.cookies, so a real test login stores the cookie but GET /auth/me returns 401. Cookie lacks Secure, defaults Path=/, there is no backend logout/delete_cookie, frontend still uses localStorage and Authorization, CORS remains wildcard with credentials, and no CSRF defense exists. OAuth2 tokenUrl now points to /login even though /login no longer returns OAuth token JSON, so Swagger Authorize will fail. python3 -m pytest -q tests/unit/test_auth.py: 5 passed, 2 failed. Required completion: cookie-or-bearer extractor with auto_error=False, update frontend and startup probe, server logout, cookie security settings, explicit CORS/CSRF strategy, separate /token endpoint if Swagger Bearer support is retained, and update cookie-based tests.

## Outcome

- Signal: useful

## Source Nodes

- login()
- get_current_user()
- api()
- logout()
- enterApp()